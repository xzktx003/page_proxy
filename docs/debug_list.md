# Bug 修复记录

| 日期 | 问题现象 | 根因 | 修复点 | 验证 |
|------|----------|------|--------|------|
| 2026-06-10 | 添加服务后打开对应服务一片空白 | SPA 入口可被浏览器缓存，离线目标服务的代理错误也未走友好错误页；同时全局 `PORT=4000` 容易干扰默认端口选择 | 默认端口改为 39197，`start.sh` 清除全局 `PORT`，SPA fallback 增加 no-cache 头，代理错误改用 `http-proxy-middleware` v2 的 `onError` 友好页面 | `curl` 验证首页、服务 SPA 页、在线服务代理、离线服务错误页、静态资源和局域网地址 |
| 2026-06-10 | 新增的 Vite/React 类服务在代理 iframe 中打不开或显示空白 | 代理只转发 `/slug/...` 请求，但目标 HTML/JS 内的根相对资源仍指向 `/@vite/client`、`/src/...`、`/node_modules/...`，浏览器会绕过服务标识请求代理根路径 | 代理响应拦截 HTML/JS/CSS/JSON，将根相对引用改写为 `/<slug>/...`，并补充 Node 回归测试覆盖 HTML、JS import、CSS url() 重写 | `npm test`、`npm run build`、`curl http://10.30.0.22:39197/36ef74bf/` 验证 HTML 资源前缀、`curl /36ef74bf/src/main.tsx` 验证 JS import 前缀、服务监听 `0.0.0.0:39197` |
| 2026-06-10 | `coding kanban` 点击后仍加载不完整，运行时数据和实时连接异常 | 代理继续改写 JSON 响应，导致业务字段里的 `/data01/...` 路径被污染为 `/<slug>/data01/...`；同时 JS 模板字符串形式的 `${apiBaseUrl}/api`、`${wsBase()}/ws` 未加代理前缀，且主服务未处理 WebSocket upgrade | 停止改写 JSON；补充 JS 模板根路径重写；为 HTTP server 增加 WebSocket upgrade 代理到对应服务 | `npm test`、`npm run build`、`curl /36ef74bf/api/agent-sessions` 验证 JSON 真实路径、手写 WebSocket 握手验证 `/36ef74bf/ws/agent-sessions` 返回 `101 Switching Protocols` 和 snapshot |
| 2026-06-11 | `/36ef74bf` 用浏览器打开仍空白，且访问一段时间后主服务失效；添加新服务后点击也加载失败 | Playwright 复现到三类问题：Vite dev HMR 透过二级代理反复重连并 abort 模块加载；JS 宽泛字符串重写误改正则字面量，造成 `Invalid regular expression flags`；WebSocket 错误路径复用 HTTP `writeHead` 导致 Node 崩溃 | 对代理下的 `/@vite/client` 返回无 HMR shim；JS 重写收窄为 import/API/WS 模板路径，避免污染正则和业务字符串；WebSocket 错误只销毁 socket，并将业务 WS 模板改为目标服务直连；缓存 upgrade proxy 降低监听器堆积 | Playwright 打开 `http://10.30.0.22:39197/36ef74bf`，iframe 内出现 `电脑端 Coding Kanban` 和 18 个会话；Playwright UI 添加临时服务 `pw-kanban-test`、点击打开成功且无错误；`npm test`、`npm run build`、60 秒监听稳定性检查 |
| 2026-06-11 | `/36ef74bf` 内容已出现但页面像未加载样式的纯 HTML | 为禁用 HMR 返回的 Vite client shim 将 `updateStyle()` 实现为空函数，目标 Vite CSS 模块调用后没有向页面注入样式 | shim 中实现 `updateStyle(id, content)` 和 `removeStyle(id)`，按 Vite dev client 约定维护 `style[data-vite-dev-id]`，保留禁用 HMR 行为 | Playwright 截图 `/tmp/page_proxy_final_styled.png` 可见深色 Kanban 样式；iframe 内存在 1 个 Vite style 标签且 CSS 长度约 82KB；补充 `createViteClientShim()` 回归测试 |
| 2026-06-11 | 点击 `调试号` 卡片后目标返回 `{"message":"Unauthorized"}` | 卡片固定 iframe 到 `/<slug>/`，而目标 `10.30.0.22:9090` 根路径不开放，实际入口在 `/ui/#/proxies` | 新增服务 `entryPath` 默认访问路径，点击卡片、切换服务和分享链接使用该路径；调试号配置为 `/ui/#/proxies` | `npm test`、`npm run build`、API 更新调试号配置、`curl /debug-proxies/ui/` 返回 200 |
| 2026-06-11 | `mihomo` 直接访问 `/ui/` 可用，但放进服务管理后点击和页面操作失效 | 服务默认入口被改回 `/`，且 mihomo/yacd 前端会从 iframe 内请求根路径 API（如 `/configs`、`/proxies`、`/rules`），这些请求在总端口上会命中 Page Proxy 自身而不是目标服务 | 将 `mihomo` 默认访问路径改回 `/ui/`；增加基于 Referer 的根路径 API 归属代理，将来自 `/<slug>/...` iframe 的根 API 请求转发回对应目标服务 | `npm test`；`curl /cea6d5de/ui/` 与页面资源返回 200；带 Referer 的 `/configs`、`/proxies` 返回目标服务 JSON 状态 |
| 2026-06-11 | 通过服务管理打开 `mihomo` 像一个新实例，和直接访问 `9090/ui/` 已保存配置不一致 | yacd 将配置保存在浏览器 localStorage 的 `yacd.haishan.me` 键；`http://10.30.0.22:9090` 与 `http://10.30.0.22:39197` 是不同 origin，浏览器按安全边界隔离存储，代理页无法读取直连页配置 | 增加服务打开方式 `openMode`，将 `mihomo` 配置为直连打开，点击卡片进入 `http://10.30.0.22:9090/ui/`，复用原 origin 下的本地配置 | `npm test`、`npm run build`、API 确认 `mihomo.openMode=direct`、直连 URL 返回 200 |
| 2026-06-12 | 通过 UI 修改服务配置（如将协议改为 HTTPS）总是失败 | API 路由缺少 `express.json()` 中间件，POST/PUT 请求 `req.body` 为 `undefined`，解构赋值直接抛 TypeError；同时健康检查始终用 `http.request()` 无法检测 HTTPS 服务，且 HTTPS 代理和健康检查缺少 `rejectUnauthorized: false` 导致自签名证书目标始终失败 | 为 services router 添加 `router.use(express.json())`；健康检查按协议选择 `http/https` 模块发起请求；HTTPS agent 添加 `rejectUnauthorized: false`；代理中间件添加 `secure: false` | `node --test` 全部通过，新增 3 个测试用例覆盖 JSON body 解析、协议模块选择、rejectUnauthorized |
